

<div class="container">
    <div class="row">
        <div class="card card-body bg-dark">
            <h2 class="text-center text-white">Edita un registro</h2>
            <form action="/edit/{{task._id}}" method="post">

            <input class="form-control mt-3" type="text" name="title" value="{{task.title}}" id="">

            <textarea class="form-control mt-3" name="description" id="description"rows="3">{{task.description}}
            </textarea>

            <div class="d-flex justify-content-center">
                <button class="btn btn-warning mt-3 w-50">Actualizar</button>

            </div>

        </div>
    </div>
</div>

</form>